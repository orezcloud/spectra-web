import {DefaultSection} from '../../layouts/section-layouts';


export default function Header() {
    return (
        <DefaultSection name={'header'} padding={'none'}>

        </DefaultSection>
    );
}
